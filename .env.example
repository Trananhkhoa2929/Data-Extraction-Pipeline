# PostgreSQL connection string (same DB as Project 1's AI dashboard)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/karaoke_analytics

# Path to raw branch data files
RAW_DATA_DIR=./raw_branch_data

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
